name: Build APKs
on:
  workflow_dispatch:
    inputs:
      apps:
        description: 'Specific apps to build (comma-separated, e.g. "WhatsApp,Reddit")'
        required: false
        type: string
        default: ''
      force_build:
        description: 'Build all apps (skip update check)'
        required: false
        type: boolean
        default: false
  workflow_call:
    inputs:
      apps:
        required: false
        type: string
        default: ''
      force_build:
        required: false
        type: boolean
        default: false
      filter_changelog:
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  prepare:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
      version: ${{ steps.version.outputs.version }}
      prerelease: ${{ steps.set-matrix.outputs.prerelease }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 1
          submodules: true

      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78  # v7.6.0
        with:
          python-version: "3.13"
          enable-cache: true
          quiet: true

      - name: Install dependencies
        run: uv sync --frozen

      - name: Prepare version and draft release
        id: version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VER="$(date +%y.%m.%d)"
          echo "version=${VER}" >> "$GITHUB_OUTPUT"
          if gh release view "$VER" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release edit "$VER" --draft --repo "$GITHUB_REPOSITORY"
          else
            gh release create "$VER" --draft --title "$VER" --notes "" --repo "$GITHUB_REPOSITORY"
          fi

      - name: Set matrix based on release group
        id: set-matrix
        env:
          APPS_INPUT: ${{ inputs.apps }}
          FORCE_BUILD: ${{ inputs.force_build }}
          FILTER_CHANGELOG: ${{ inputs.filter_changelog }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          RESULT="$(uv run python -m src.scripts.matrix get-build-matrix)"
          echo "matrix=$(echo "$RESULT" | jq -c '{include}')" >> "$GITHUB_OUTPUT"
          echo "prerelease=$(echo "$RESULT" | jq -r '.prerelease')" >> "$GITHUB_OUTPUT"

  run:
    needs: prepare
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write # <-- Added permission to allow cache deletion
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare.outputs.matrix) }}

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          submodules: true

      - name: Install java
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95  # v5.6.0
        with:
          distribution: "temurin"
          java-version: "21"

      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78  # v7.6.0
        with:
          python-version: "3.13"
          enable-cache: true
          quiet: true

      - name: Install dependencies
        run: uv sync --frozen

      - name: Restore stock APKs
        uses: actions/cache/restore@v4
        with:
          path: unmodified-apks/
          key: apk-cache-${{ matrix.id }}-${{ github.run_id }}
          restore-keys: |
            apk-cache-${{ matrix.id }}-

      - name: Build APKs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_PASS: ${{ secrets.KEYSTORE_PASS }}
          KEYSTORE_ALIAS: ${{ secrets.KEYSTORE_ALIAS }}
        run: uv run main.py ${{ matrix.id }} ${{ matrix.arch }}

      - name: Save stock APKs
        if: success()
        uses: actions/cache/save@v4
        with:
          path: unmodified-apks/
          key: apk-cache-${{ matrix.id }}-${{ hashFiles('unmodified-apks/*') }}

      - name: Cleanup old caches
        if: success()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh extension install actions/gh-actions-cache
          echo "Fetching list of cache keys"
          cacheKeysForPR=$(gh actions-cache list -R $GITHUB_REPOSITORY --key apk-cache-${{ matrix.id }}- -L 100 | cut -f 1 )
          set +e
          for cacheKey in $cacheKeysForPR
          do
              if [ "$cacheKey" != "apk-cache-${{ matrix.id }}-${{ hashFiles('unmodified-apks/*') }}" ]; then
                  gh actions-cache delete $cacheKey -R $GITHUB_REPOSITORY --confirm
              fi
          done

      - name: Upload APKs to draft release
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.version }}
        run: |
          shopt -s nullglob
          apks=(./build/*.apk)
          if [[ ${#apks[@]} -eq 0 ]]; then
            echo "No APKs to upload, skipping"
            exit 0
          fi
          for i in {1..5}; do
            gh release upload "$TAG" "${apks[@]}" --clobber --repo "$GITHUB_REPOSITORY" && exit 0
            sleep 5
          done
          exit 1

      - name: Upload build log to draft release
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.version }}
          LABEL: ${{ matrix.id }}${{ matrix.arch && format('-{0}', matrix.arch) || '' }}
        run: |
          [[ -f build.md ]] || exit 0
          [[ -s build.md ]] || exit 0
          cp build.md "build-${LABEL}.md"
          for i in {1..5}; do
            gh release upload "$TAG" "build-${LABEL}.md" --clobber --repo "$GITHUB_REPOSITORY" && exit 0
            sleep 5
          done
          exit 1

      - name: Upload patches info to draft release
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.version }}
          LABEL: ${{ matrix.id }}${{ matrix.arch && format('-{0}', matrix.arch) || '' }}
        run: |
          [[ -f patches_info.json ]] || exit 0
          cp patches_info.json "patches_info-${LABEL}.json"
          for i in {1..5}; do
            gh release upload "$TAG" "patches_info-${LABEL}.json" --clobber --repo "$GITHUB_REPOSITORY" && exit 0
            sleep 5
          done
          exit 1

      - name: Upload versions info to draft release
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.version }}
          LABEL: ${{ matrix.id }}${{ matrix.arch && format('-{0}', matrix.arch) || '' }}
        run: |
          [[ -f build.json ]] || exit 0
          cp build.json "versions_info-${LABEL}.json"
          for i in {1..5}; do
            gh release upload "$TAG" "versions_info-${LABEL}.json" --clobber --repo "$GITHUB_REPOSITORY" && exit 0
            sleep 5
          done
          exit 1

  release:
    needs: [prepare, run]
    if: ${{ !cancelled() }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          submodules: true

      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78  # v7.6.0
        with:
          python-version: "3.13"
          enable-cache: true
          quiet: true

      - name: Install dependencies
        run: uv sync --frozen

      - name: Check if any APKs were uploaded
        id: check
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.version }}
        run: |
          has=$(gh release view "$TAG" --repo "$GITHUB_REPOSITORY" --json assets --jq '[.assets[].name | select(endswith(".apk"))] | length > 0')
          echo "has_apks=${has}" >> "$GITHUB_OUTPUT"

      - name: Download logs and patches from draft release
        if: steps.check.outputs.has_apks == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.version }}
        run: |
          mkdir -p logs patches versions
          gh release download "$TAG" --repo "$GITHUB_REPOSITORY" --pattern "build-*.md" --dir logs
          gh release download "$TAG" --repo "$GITHUB_REPOSITORY" --pattern "patches_info-*.json" --dir patches || true
          gh release download "$TAG" --repo "$GITHUB_REPOSITORY" --pattern "versions_info-*.json" --dir versions || true
          uv run python -c "import json, glob, pathlib; curr = {'success':[]}; [curr['success'].extend(json.loads(pathlib.Path(f).read_text()).get('success', [])) for f in glob.glob('versions/*.json')]; pathlib.Path('current_versions.json').write_text(json.dumps(curr, indent=2))"
          gh release view "$TAG" --repo "$GITHUB_REPOSITORY" --json assets --jq '.assets[] | select(.name | startswith("build-") or startswith("patches_info-") or startswith("versions_info-")) | .apiUrl' | xargs -I{} gh api --method DELETE "{}"

      - name: Combine logs and finalize release
        if: steps.check.outputs.has_apks == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.version }}
          PRERELEASE: ${{ needs.prepare.outputs.prerelease }}
        run: |
          uv run python -m src.scripts.logs combine-logs logs current_versions.json | sed "s/{TAG}/$TAG/g" > final.md
          gh release edit "$TAG" --repo "$GITHUB_REPOSITORY" --draft=false --title "$TAG" --notes-file final.md

      - name: Send Telegram notification
        if: steps.check.outputs.has_apks == 'true' && env.TG_TOKEN != '' && env.TG_CHAT != ''
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT: ${{ secrets.TG_CHAT }}
        run: uv run python -m src.scripts.telegram notify

      - name: Delete empty draft if no APKs
        if: steps.check.outputs.has_apks != 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.version }}
        run: gh release delete "$TAG" --repo "$GITHUB_REPOSITORY" --yes || true

      - name: Update patches info and README
        if: steps.check.outputs.has_apks == 'true'
        run: |
          uv run python -c "import json, pathlib; main_p = pathlib.Path('versions_info.json'); main_d = json.loads(main_p.read_text()) if main_p.exists() else {'success':[]}; curr_d = json.loads(pathlib.Path('current_versions.json').read_text()); curr_apps = {i['app'] for i in curr_d.get('success', []) if 'app' in i}; merged = [i for i in main_d.get('success', []) if i.get('app') not in curr_apps] + curr_d.get('success', []); main_d['success'] = merged; main_p.write_text(json.dumps(main_d, indent=2))"
          uv run python -c "import json, glob, pathlib; p_path = pathlib.Path('patches_info.json'); p_data = json.loads(p_path.read_text()) if p_path.exists() else {}; [p_data.update(json.loads(pathlib.Path(f).read_text())) for f in glob.glob('patches/*.json')]; p_path.write_text(json.dumps(p_data, indent=2))"
          uv run python -m src.scripts.readme update
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add patches_info.json versions_info.json README.md obtainium.json
          git diff --cached --quiet || git commit -m "docs: update patches and versions info [skip ci]"
          git push || true
